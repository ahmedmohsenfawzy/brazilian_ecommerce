version: 2

sources:
  - name: brazilian_ecommerce
    database: brazilian_ecommerce  # This is the catalog
    schema: raw       # This is the schema
    tables:
      - name: customers
        identifier: raw_customers

      - name: geolocation
        identifier: raw_geolocation

      - name: order_items
        identifier: raw_order_items

      - name: orders
        identifier: raw_orders

      - name: payments
        identifier: raw_payments

      - name: product_category_translation
        identifier: raw_product_category_translation

      - name: products
        identifier: raw_products

      - name: reviews
        identifier: raw_reviews

      - name: sellers
        identifier: raw_sellers